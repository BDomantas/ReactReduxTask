{
  "name": "exercise_task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-client": "cd client && npm install",
    "intall-api": "cd api && npm install",
    "start": "concurrently \"cd api && node index.js\" \"cd client && npm start\""
  },
  "author": "Domantas",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
